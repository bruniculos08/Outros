#include <stdio.h>
#include <cs50.h>
#include <stdlib.h>
#include <string.h>


int main(int argc, string argv[]) //remeber to check each individual character to see if it's a integer
{char s = argv[1];
 int x = atoi (&s);
 int n = strlen(&argv[1]);
 int y = n+x;
    //if (x>=48 && x<=57)
   {
   printf(" x=%i, n=%i, n+x=%i \n", x, n, y);
   }
    //else
    //{
    //  printf("ERROR \n");
   // }
}